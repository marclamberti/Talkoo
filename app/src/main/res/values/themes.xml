<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/FlatActionBar</item>
    </style>

    <!-- LoginActivity theme -->
    <style name="LoginActivityTheme" parent="AppTheme">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowTitleSize">0dp</item>
    </style>

    <!-- HomeActivity theme -->
    <style name="HomeActivityTheme" parent="AppTheme">
        <item name="android:actionBarItemBackground">@drawable/background_action_bar</item>
        <item name="android:actionBarStyle">@style/FlatActionBarHiddenAppLogo</item>
    </style>

</resources>
